name: Artifacts

on:
  workflow_dispatch:
    
jobs:
  artifact-producer:
    name: produce artifact
    runs-on: ubuntu-latest
    steps:
      - name: Create artifact file
        run: |
          echo "This will be stored as an artifact!" > artifact.txt
          echo "Generated by ${{ job.id }} job on $(date)" >> artifact.txt
  
